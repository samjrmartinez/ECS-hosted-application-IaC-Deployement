<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="30">
  <Appenders>
    <Console name="console" target="SYSTEM_OUT" follow="true">
      <PatternLayout
        pattern="[%d{yyyy-MM-dd HH:mm:ss.SSS}] [%thread] [%highlight{%-5level}] %style{%t}{bright,blue} %style{%C{1.}}{bright,yellow}: %msg%n%throwable"/>
    </Console>
  </Appenders>

  <Loggers>
    <!-- LOG everything at INFO level -->
    <Root level="info">
      <AppenderRef ref="console"/>
    </Root>
    <!-- LOG "com.sammartinez.example" at TRACE level -->
    <Logger name="com.sammartinez.example" level="trace"></Logger>
  </Loggers>
</Configuration>
